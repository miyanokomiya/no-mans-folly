{
  "name": "no-mans-folly-mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "esbuild server.ts --bundle --platform=node --packages=external --format=esm --outfile=dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "playwright": "^1.51.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
